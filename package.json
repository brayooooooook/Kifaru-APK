{
  "name": "long-haul-africa",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Long Haul Africa — original right-hand-drive American conventional truck simulator for left-side traffic.",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 5173",
    "lint": "tsc --noEmit",
    "test": "tsx src/game/tests/sim.test.ts",
    "cap:add": "cap add android",
    "cap:sync": "npm run build && cap sync android",
    "android:debug": "npm run build && cap sync android && cd android && ./gradlew assembleDebug",
    "android:release": "npm run build && cap sync android && cd android && ./gradlew assembleRelease bundleRelease",
    "apk": "npm run build && node scripts/make-apk.mjs"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/status-bar": "^6.0.2",
    "cannon-es": "^0.20.0",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "@types/node": "^22.14.0",
    "@types/three": "^0.185.4",
    "apk_sign_ts": "^1.0.1",
    "jszip": "^3.10.1",
    "selfsigned": "^5.5.0",
    "tsx": "^4.21.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.3"
  }
}
